import { TypeRedezeitPipe } from './type-redezeit.pipe';

describe('TypeRedezeitPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeRedezeitPipe();
    expect(pipe).toBeTruthy();
  });
});
